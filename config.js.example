var cfg = [];

cfg.twitch = {
    username: "USERNAMEHERE", // Twitch username to connect to
    oauthtoken: "OAUTHHERE", // Twitch OAuth Password (Generated here: https://twitchapps.com/tmi)
    channels: [ "#CHANNEL", "#CHANNEL2" ], // Channels to connect to with a hashtag (Example: #thetetrabyte)
    prefix: "!" // Chat prefix
};

cfg.options = {
    logging: {
        commands: true, // Command Logging
        chat: true // Chat Logging
    }
};

module.exports = cfg;
